import 'package:flutter/material.dart';

class AddSpendingsPage extends StatelessWidget {
  const AddSpendingsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
